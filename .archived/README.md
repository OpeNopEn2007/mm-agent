# Archived Materials

`.archived/` 只保存非活跃历史资产。这里的文件可以用于回溯决策和实现来源，但不是当前项目指令、运行时入口或实施计划。

- `legacy-claude-codex-plugin/`：`v0.x` Claude/Codex Plugin 方向及其历史研究、Agents、Skills、Hooks 和配置。
- `implementation-records/`：已完成里程碑的简短历史记录。当前工作只服从根 `PLAN.md`、canonical 文档和 `HANDOFF.md`。

不要从归档恢复旧入口、旧工作流或隐藏规划状态。需要重新采用任何历史设计时，必须先写入当前 canonical 文档并通过新的显式验收。

---
phase: 02-problem-analysis-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.claude/skills/mm-agent/parse-problem.md, requirements.txt]
autonomous: true
requirements: [PROB-02]
user_setup:
  - service: pypi
    why: "PyMuPDF for PDF text extraction"
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "User can provide Markdown/TXT format problem file and system parses it"
    - "parse-problem skill is auto-discovered by Claude Code CLI"
    - "PyMuPDF is installed for PDF parsing in later tasks"
  artifacts:
    - path: ".claude/skills/mm-agent/parse-problem.md"
      provides: "Problem parsing skill"
      contains: "name: parse-problem"
      min_lines: 100
    - path: "requirements.txt"
      provides: "Python dependencies"
      contains: "PyMuPDF"
      min_lines: 1
  key_links:
    - from: ".claude/skills/mm-agent/SKILL.md"
      to: ".claude/skills/mm-agent/parse-problem.md"
      via: "Skill tool invocation from coordinator"
      pattern: "Skill.*parse-problem"
    - from: "requirements.txt"
      to: "Python environment"
      via: "pip install"
      pattern: "PyMuPDF"
---

<objective>
Install PyMuPDF dependency and create parse-problem skill foundation for file format detection and text extraction.

Purpose: Establish the foundation for problem parsing by installing required dependencies and creating the skill skeleton that handles PDF/MD/TXT file detection and text extraction.

Output: Working parse-problem.md skill skeleton with PyMuPDF installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-problem-analysis-pipeline/02-RESEARCH.md
@.claude/skills/mm-agent/SKILL.md
@.claude/skills/mm-agent/coordinator.md
@tests/fixtures/simple.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PyMuPDF dependency</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (check if exists)
    - .planning/phases/02-problem-analysis-pipeline/02-RESEARCH.md (PyMuPDF version)
  </read_first>
  <action>
    Install PyMuPDF (fitz) v1.27.2.2 for PDF text extraction.

    Create requirements.txt with the following content:
    ```
    PyMuPDF==1.27.2.2
    ```

    Then run installation:
    ```bash
    pip install PyMuPDF==1.27.2.2
    ```

    Verify installation:
    ```bash
    python3 -c "import fitz; print(f'PyMuPDF version: {fitz.VersionBind}')"
    ```

    PyMuPDF is the standard for high-performance PDF text extraction with Python 3.10+ compatibility.
  </action>
  <verify>
    <automated>test -f requirements.txt && grep -q "PyMuPDF" requirements.txt && python3 -c "import fitz; print('OK')" 2>&1 | grep -q "OK"</automated>
  </verify>
  <done>requirements.txt exists with PyMuPDF==1.27.2.2, pip install completes without errors, and Python can import fitz module</done>
</task>

<task type="auto">
  <name>Task 2: Create parse-problem.md skill skeleton</name>
  <files>.claude/skills/mm-agent/parse-problem.md</files>
  <read_first>
    - .claude/skills/mm-agent/parse-problem.md (check if exists)
    - .claude/skills/mm-agent/SKILL.md (frontmatter and process patterns)
    - .claude/skills/mm-agent/coordinator.md (orchestration pattern)
    - .planning/phases/02-problem-analysis-pipeline/02-RESEARCH.md (skill structure reference)
  </read_first>
  <action>
    Create parse-problem.md skill skeleton with frontmatter and basic process structure.

    Frontmatter:
    ```yaml
    ---
    name: parse-problem
    description: Parse unstructured competition problems and extract structured definition
    ---
    ```

    Objective section:
    ```markdown
    <objective>
    Parse problem file (PDF/MD/TXT), extract structured components (title, background, questions, constraints, objectives, keywords, summary), and output problem.md to .planning/memory/.

    This skill handles file format detection, text extraction, and semantic analysis using Claude Code's LLM capabilities.
    </objective>
    ```

    Execution context:
    ```markdown
    <execution_context>
    @.claude/skills/mm-agent/coordinator.md
    @.planning/phases/02-problem-analysis-pipeline/02-RESEARCH.md
    </execution_context>
    ```

    Process section skeleton (steps 1-2 for this plan, steps 3-6 added in later plans):
    ```markdown
    <process>
    ## Step 1: Detect file format
    Parse file extension from problem_path to determine parsing strategy:
    - .pdf → Use PyMuPDF (fitz)
    - .md or .txt → Use standard file I/O

    ## Step 2: Extract raw text
    For PDF (implemented in Plan 02-02):
    - Use PyMuPDF to open document
    - Iterate through all pages
    - Extract text from each page using page.get_text()
    - Concatenate with newline separators

    For MD/TXT:
    - Open file with utf-8 encoding
    - Read entire content
    - Fallback to latin-1 if utf-8 fails

    ## Step 3-6: Structured extraction and output
    (To be implemented in Plans 02-03 and 02-04)
    </process>
    ```

    Notes section:
    ```markdown
    <notes>
    **Skill auto-discovery:**
    This skill is automatically discovered by Claude Code from .claude/skills/mm-agent/parse-problem.md.

    **Coordinator integration:**
    The coordinator skill invokes parse-problem with the problem file path after initial validation in SKILL.md.

    **Output format:**
    problem.md is written to .planning/memory/ for use by Phase 3 (Task Decomposition).
    </notes>
    ```

    The skill should be ~100-120 lines with clear step-by-step instructions.
  </action>
  <verify>
    <automated>test -f .claude/skills/mm-agent/parse-problem.md && grep -q "^name: parse-problem$" .claude/skills/mm-agent/parse-problem.md && grep -q "<objective>" .claude/skills/mm-agent/parse-problem.md && grep -q "<process>" .claude/skills/mm-agent/parse-problem.md && grep -q "Step 1:" .claude/skills/mm-agent/parse-problem.md && wc -l < .claude/skills/mm-agent/parse-problem.md | awk '{print $1}' | xargs -I {} test {} -ge 100</automated>
  </verify>
  <done>parse-problem.md exists with valid frontmatter (name: parse-problem), objective section explaining file parsing, process section with at least 2 steps (format detection, text extraction), notes section with auto-discovery and coordinator integration notes, and minimum 100 lines of content</done>
</task>

</tasks>

<verification>
Run smoke test with simple.md fixture to verify skill discovery and basic parsing:
```bash
# Note: Full smoke test requires Plan 02-02-02-03 completion
# This verification only checks skill file structure
test -f .claude/skills/mm-agent/parse-problem.md
grep -q "name: parse-problem" .claude/skills/mm-agent/parse-problem.md
```
</verification>

<success_criteria>
1. PyMuPDF is installed and importable
2. requirements.txt exists with PyMuPDF==1.27.2.2
3. parse-problem.md skill exists with valid frontmatter
4. parse-problem.md has objective, process (at least 2 steps), and notes sections
5. Skill is auto-discoverable by Claude Code CLI
</success_criteria>

<output>
After completion, create `.planning/phases/02-problem-analysis-pipeline/02-01-SUMMARY.md`
</output>
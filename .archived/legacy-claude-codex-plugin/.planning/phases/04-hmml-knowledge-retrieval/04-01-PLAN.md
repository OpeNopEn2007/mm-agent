---
phase: 04-hmml-knowledge-retrieval
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Test file exists with stub tests for HMML retrieval"
    - "Test fixtures for sample query and embeddings are available"
    - "Test framework can be invoked via pytest command"
  artifacts:
    - path: "tests/test_hmml_retrieval.py"
      provides: "Test scaffolds for KNOW-01, KNOW-02, KNOW-03"
      min_lines: 100
    - path: "tests/fixtures/query-sample.txt"
      provides: "Sample task description for retrieval testing"
      min_lines: 1
    - path: "tests/fixtures/embeddings-sample.npy"
      provides: "Sample embeddings for fast testing"
      contains: "numpy array"
  key_links:
    - from: "tests/test_hmml_retrieval.py"
      to: "pytest"
      via: "pytest test discovery"
      pattern: "def test_"
---

<objective>
Create test scaffolds for HMML retrieval functionality to enable test-driven development.

Purpose: Establish test infrastructure before implementation, following Nyquist rule that every task must have automated verification.

Output: Test file with stubs, fixtures for sample data
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/VALIDATION.md
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with stub tests for HMML retrieval</name>
  <files>tests/test_hmml_retrieval.py</files>
  <read_first>
    tests/conftest.py - To understand fixture patterns used in Phase 3
    .planning/VALIDATION.md - For test requirements and naming conventions
  </read_first>
  <action>
    Create tests/test_hmml_retrieval.py with the following test stubs:

    ```python
    #!/usr/bin/env python3
    """Test suite for HMML retrieval functionality."""

    import pytest
    import numpy as np
    import json
    from pathlib import Path


    # Tests for KNOW-01: Load precomputed HMML embeddings
    def test_load_embeddings(tmp_path):
        """Test that HMML embeddings can be loaded from numpy file."""
        # Create sample embeddings file (97 methods, 1024-dim)
        embeddings = np.random.rand(97, 1024).astype(np.float32)
        embeddings_path = tmp_path / "hmml-embeddings.npy"
        np.save(embeddings_path, embeddings)

        # Load and verify
        loaded = np.load(embeddings_path)
        assert loaded.shape == (97, 1024)
        assert loaded.dtype == np.float32


    # Tests for KNOW-02: Retrieve Top-K relevant methods
    def test_retrieve_methods(tmp_path):
        """Test that top-k relevant methods are retrieved based on query."""
        # Create sample embeddings (97 methods, 1024-dim)
        method_embeddings = np.random.rand(97, 1024).astype(np.float32)
        query_embedding = np.random.rand(1024).astype(np.float32)

        # Create sample HMML structure
        hmml_data = [
            {
                "method_class": "Operations Research",
                "children": [
                    {
                        "method_class": "Linear Programming",
                        "children": [
                            {
                                "method": "Linear Programming (LP)",
                                "description": "LP is a mathematical optimization method..."
                            }
                        ]
                    }
                ]
            }
        ]

        # Create embedding meta
        embedding_meta = {
            "method_ids": ["0"] * 97,
            "embedding_dim": 1024
        }

        # Save files
        np.save(tmp_path / "hmml-embeddings.npy", method_embeddings)
        with open(tmp_path / "hmml.json", 'w') as f:
            json.dump(hmml_data, f)
        with open(tmp_path / "embedding-meta.json", 'w') as f:
            json.dump(embedding_meta, f)

        # TODO: Implement retrieval logic and verify results
        # For now, just assert files exist
        assert (tmp_path / "hmml-embeddings.npy").exists()
        assert (tmp_path / "hmml.json").exists()
        assert (tmp_path / "embedding-meta.json").exists()


    # Tests for KNOW-03: Output retrieved-methods.json
    def test_output_format(tmp_path):
        """Test that retrieval results are output in correct JSON format."""
        output_path = tmp_path / "retrieved-methods.json"

        # TODO: Implement retrieval and generate output
        # For now, create sample output to validate format
        sample_output = {
            "query": "建立网球比赛动量预测模型",
            "methods": [
                {
                    "domain": "Prediction",
                    "subdomain": "Time Series",
                    "method": "ARIMA",
                    "score": 0.85,
                    "core_idea": "ARIMA模型通过自回归、差分和移动平均来...",
                    "application": "适用于时间序列预测、趋势分析..."
                }
            ],
            "top_k": 6,
            "timestamp": "2026-04-11T12:00:00Z"
        }

        with open(output_path, 'w') as f:
            json.dump(sample_output, f, indent=2, ensure_ascii=False)

        # Verify format
        with open(output_path, 'r') as f:
            result = json.load(f)

        assert "query" in result
        assert "methods" in result
        assert "top_k" in result
        assert "timestamp" in result
        assert isinstance(result["methods"], list)
    ```

    Follow pytest conventions and match fixture patterns from conftest.py.
  </action>
  <verify>pytest tests/test_hmml_retrieval.py -x -v</verify>
  <done>
    - tests/test_hmml_retrieval.py exists with 3 test functions
    - Tests cover KNOW-01 (load_embeddings), KNOW-02 (retrieve_methods), KNOW-03 (output_format)
    - pytest can run the tests (even if some fail pending implementation)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures for sample query and embeddings</name>
  <files>tests/fixtures/query-sample.txt, tests/fixtures/embeddings-sample.npy</files>
  <read_first>
    tests/fixtures/ - To understand existing fixture patterns
  </read_first>
  <action>
    Create two fixture files:

    1. tests/fixtures/query-sample.txt:
       ```
       建立网球比赛动量预测模型
       ```

    2. tests/fixtures/embeddings-sample.npy:
       Use numpy to create a small embeddings array:
       - Shape: (10, 1024) - 10 sample methods for fast testing
       - dtype: float32 (matches BGE-m3 output)
       - Values: Random but deterministic for reproducible tests

    Create via Python script:
    ```python
    import numpy as np
    np.random.seed(42)  # For reproducibility
    embeddings = np.random.rand(10, 1024).astype(np.float32)
    np.save('tests/fixtures/embeddings-sample.npy', embeddings)
    ```
  </action>
  <verify>
    test -f tests/fixtures/query-sample.txt && test -f tests/fixtures/embeddings-sample.npy && python3 -c "import numpy as np; arr = np.load('tests/fixtures/embeddings-sample.npy'); print(f'Shape: {arr.shape}, dtype: {arr.dtype}')"
  </verify>
  <done>
    - tests/fixtures/query-sample.txt exists with sample task description
    - tests/fixtures/embeddings-sample.npy exists with shape (10, 1024), dtype float32
    - Fixtures can be loaded by tests for fast testing
  </done>
</task>

</tasks>

<verification>
```bash
# Run all Phase 4 tests
pytest tests/test_hmml_retrieval.py -x -v

# Verify fixtures exist
ls -la tests/fixtures/query-sample.txt
ls -la tests/fixtures/embeddings-sample.npy
```
</verification>

<success_criteria>
- Test file created with 3 test stubs covering all requirements
- Fixtures created for sample query and embeddings
- Tests can be invoked via pytest (some may fail pending implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/04-hmml-knowledge-retrieval/04-01-SUMMARY.md`
</output>
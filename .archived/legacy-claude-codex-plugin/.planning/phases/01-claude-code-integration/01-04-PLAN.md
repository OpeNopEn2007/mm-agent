---
phase: 01-claude-code-integration
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified: [tests/fixtures/simple.md]
autonomous: false
requirements: [INTG-02]
user_setup: []
must_haves:
  truths:
    - "System inherits Claude Code's model configuration without requiring separate API keys"
    - "Workflow entry point is verified and functional"
    - "Skills and Agents are properly discovered and registered"
  artifacts:
    - path: "tests/fixtures/simple.md"
      provides: "Minimal test problem for smoke testing"
      min_lines: 10
  key_links:
    - from: "/mm-agent command"
      to: "tests/fixtures/simple.md"
      via: "command line invocation"
      pattern: "--problem tests/fixtures/simple.md"
---

<objective>
Verify the workflow entry point, test model inheritance, and create smoke test fixtures.

Purpose: Ensure users can invoke `/mm-agent --problem <file>` and receive initial workflow response. Verify that the system inherits Claude Code's model configuration without requiring separate API keys. Create minimal test fixtures for smoke testing in later phases.

Output: Verified workflow entry point with smoke test fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@planning/STATE.md
@.planning/phases/01-claude-code-integration/01-CONTEXT.md
@.planning/phases/01-claude-code-integration/01-RESEARCH.md
@.planning/phases/01-claude-code-integration/01-VALIDATION.md
@.planning/phases/01-claude-code-integration/01-01-SUMMARY.md
@.planning/phases/01-claude-code-integration/01-02-SUMMARY.md
@.planning/phases/01-claude-code-integration/01-03-SUMMARY.md
@IDEA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test fixture files</name>
  <files>tests/fixtures/simple.md, tests/fixtures/multi-task.md, tests/fixtures/prediction.md</files>
  <read_first>
    - tests/fixtures/simple.md (check if exists)
    - tests/fixtures/multi-task.md (check if exists)
    - tests/fixtures/prediction.md (check if exists)
  </read_first>
  <action>
    Create three test fixture files for smoke testing different problem types:

    1. Create tests/fixtures/simple.md (simple optimization problem):
    ```markdown
    # Simple Optimization Problem

    ## Problem Statement

    A factory produces two types of products: Product A and Product B.
    Each Product A requires 2 hours of labor and 3 hours of machine time.
    Each Product B requires 4 hours of labor and 2 hours of machine time.

    The factory has 40 hours of labor and 30 hours of machine time available per day.
    Product A yields a profit of $50 per unit.
    Product B yields a profit of $60 per unit.

    ## Goal

    Determine how many units of each product should be produced to maximize profit.

    ## Questions

    1. Formulate this as a linear programming problem.
    2. Solve for the optimal production quantities.
    3. Calculate the maximum profit.

    ## Constraints

    - Labor hours: 2A + 4B ≤ 40
    - Machine hours: 3A + 2B ≤ 30
    - Non-negativity: A ≥ 0, B ≥ 0
    - A and B must be integers

    ## Objectives

    - Maximize profit: P = 50A + 60B
    - Find integer solution
    - Verify constraints are satisfied

    ## Keywords

    Linear Programming, Optimization, Integer Programming, Resource Allocation
    ```
    (~35 lines)

    2. Create tests/fixtures/multi-task.md (multi-task dependency problem):
    ```markdown
    # Tennis Match Momentum Analysis

    ## Problem Background

    In professional tennis, momentum shifts between players can significantly impact match outcomes.
    This problem analyzes momentum patterns to predict future match performance.

    ## Problem Statement

    Given historical match data from ATP/WTA tournaments, analyze player momentum and build
    a prediction model for match outcomes.

    ## Research Goals

    1. Analyze momentum patterns from historical match data
    2. Identify key factors affecting momentum shifts
    3. Build a momentum-based prediction model
    4. Validate the model on recent tournament data

    ## Subproblems

    ### Task 1: Data Preprocessing and Feature Extraction
    - Clean and normalize historical match data
    - Extract momentum indicators (winning streaks, set streaks, etc.)
    - Create feature vectors for momentum analysis
    - Output: Cleaned dataset with momentum features

    ### Task 2: Momentum Pattern Analysis
    - Analyze temporal patterns in momentum shifts
    - Identify critical momentum thresholds
    - Determine momentum persistence characteristics
    - Output: Momentum pattern analysis results

    ### Task 3: Prediction Model Development
    - Build machine learning model using momentum features
    - Train on historical data with cross-validation
    - Tune hyperparameters for optimal performance
    - Output: Trained prediction model with performance metrics

    ### Task 4: Model Validation and Analysis
    - Test model on recent tournament data
    - Compare with baseline prediction methods
    - Analyze feature importance and model interpretability
    - Output: Validation report and model analysis

    ## Constraints

    - Use at least 5 years of historical match data
    - Model should have minimum 70% prediction accuracy
    - Must handle missing data gracefully
    - Explanation of model decisions required

    ## Objectives

    - Develop interpretable momentum-based prediction model
    - Achieve competitive prediction accuracy
    - Provide insights into momentum dynamics

    ## Evaluation Criteria

    - Prediction accuracy on test set
    - Model interpretability (feature importance)
    - Handling of edge cases (missing data, rare events)
    - Computational efficiency (training and inference time)

    ## Keywords

    Tennis, Momentum Prediction, Time Series Analysis, Machine Learning, Sports Analytics
    ```
    (~60 lines)

    3. Create tests/fixtures/prediction.md (prediction with data problem):
    ```markdown
    # Stock Price Prediction

    ## Problem Statement

    Given historical stock price data for a technology company, develop a prediction model
    to forecast future prices based on technical indicators.

    ## Data Available

    - Daily closing prices for the past 5 years
    - Trading volume data
    - Market index values

    ## Goals

    1. Extract meaningful technical indicators from price data
    2. Build a time series prediction model
    3. Forecast next 30 days of prices
    4. Evaluate prediction accuracy using backtesting

    ## Constraints

    - Use time series analysis techniques
    - Model should handle non-stationary data
    - Provide confidence intervals for predictions

    ## Keywords

    Time Series, Stock Prediction, Technical Analysis, ARIMA, LSTM
    ```
    (~25 lines)

    All fixture files should follow the structure defined in the problem schema (Phase 2 output format).
  </action>
  <verify>
    <automated>test -f tests/fixtures/simple.md && grep -q "Simple Optimization Problem" tests/fixtures/simple.md && grep -q "Linear Programming" tests/fixtures/simple.md && test -f tests/fixtures/multi-task.md && grep -q "Tennis Match Momentum" tests/fixtures/multi-task.md && grep -q "Task 1:" tests/fixtures/multi-task.md && grep -q "Task 2:" tests/fixtures/multi-task.md && grep -q "Task 3:" tests/fixtures/multi-task.md && grep -q "Task 4:" tests/fixtures/multi-task.md && test -f tests/fixtures/prediction.md && grep -q "Stock Price Prediction" tests/fixtures/prediction.md && wc -l < tests/fixtures/simple.md | awk '{print $1}' | xargs -I {} test {} -ge 10 && wc -l < tests/fixtures/multi-task.md | awk '{print $1}' | xargs -I {} test {} -ge 10 && wc -l < tests/fixtures/prediction.md | awk '{print $1}' | xargs -I {} test {} -ge 10</automated>
  </verify>
  <done>Three test fixture files created: simple.md (~35 lines, single optimization problem), multi-task.md (~60 lines, multi-task with dependencies for testing DAG), prediction.md (~25 lines, prediction with data reference). All files contain required sections (Problem Statement, Goals/Questions, Constraints, Objectives, Keywords). Multi-task.md explicitly lists 4 tasks with dependencies for DAG testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete workflow entry point with Skills, Agents, Hooks, and test fixtures</what-built>
  <how-to-verify>
    **Verification Steps:**

    1. **Check Skill and Agent discovery:**
       Run: `claude skill list`
       Expected: `mm-agent` appears in the list

    2. **Verify Skill invocation:**
       Run: `/mm-agent --problem tests/fixtures/simple.md`
       Expected:
       - Workflow start banner displayed
       - File validation passed message
       - Working directories initialized message
       - Coordinator invoked message
       - Workflow completion banner (with error about missing Phase 2 - this is expected)

    3. **Verify model inheritance:**
       - Confirm the skill ran without asking for API keys
       - Confirm no configuration file for model credentials was created
       - The skill should use the current Claude Code session's model

    4. **Verify parameter parsing:**
       Run: `/mm-agent --problem tests/fixtures/simple.md --interactive`
       Expected: INTERACTIVE=true in output

    5. **Verify file validation:**
       Run: `/mm-agent --problem nonexistent.pdf`
       Expected: Error message "File not found: nonexistent.pdf"

    6. **Verify directory creation:**
       Run: `ls -la .planning/memory .planning/code .planning/output tests/fixtures`
       Expected: All directories exist

    **Success Criteria:**
    - [ ] Skill is discovered by Claude Code
    - [ ] Command invocation returns workflow response
    - [ ] No API key configuration required
    - [ ] Parameters are parsed correctly
    - [ ] File validation works
    - [ ] Directories are created
  </how-to-verify>
  <resume-signal>Type "approved" if all verification steps pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Test fixture files exist in tests/fixtures/
2. All fixtures have required problem structure sections
3. simple.md contains a single optimization problem
4. multi-task.md contains 4 tasks with dependencies for DAG testing
5. prediction.md contains a prediction problem with data reference
6. User verifies skill discovery and invocation
7. User confirms model inheritance works without API keys
8. User confirms parameter parsing works correctly
9. User confirms file validation works
10. Directories are created successfully
</verification>

<success_criteria>
1. `/mm-agent --problem tests/fixtures/simple.md` invokes the skill and displays workflow response
2. No API key configuration is required (model inherited from Claude Code)
3. All parameters (--problem, --interactive, --skip-verify, --phase) are parsed correctly
4. File validation rejects nonexistent files and unsupported formats
5. Required directories are created on skill invocation
6. Smoke test fixtures are ready for Phase 2+ testing
7. All Skills and Agents are auto-discovered by Claude Code
</success_criteria>

<output>
After completion, create `.planning/phases/01-claude-code-integration/01-04-SUMMARY.md` with:
- Smoke test fixtures created (3 files)
- Verification results (skill discovery, invocation, model inheritance)
- Parameter parsing confirmed working
- File validation confirmed working
- Directory structure verified
- Ready for Phase 2 (Problem Analysis Pipeline)
</output>
---
phase: 07-report-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_report_generation.py
  - tests/conftest.py
  - tests/fixtures/report-memory.json
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06

must_haves:
  truths:
    - "Test scaffolds exist before implementation begins"
    - "Tests cover all 6 report generation requirements"
    - "Fixtures provide realistic memory JSON structure"
  artifacts:
    - path: "tests/test_report_generation.py"
      provides: "Test scaffolds for RPT-01~06"
      min_lines: 150
    - path: "tests/fixtures/report-memory.json"
      provides: "Sample memory JSON for testing"
      min_lines: 50
  key_links:
    - from: "tests/test_report_generation.py"
      to: "tests/conftest.py"
      via: "imports fixtures"
    - from: "tests/test_report_generation.py"
      to: "tests/fixtures/report-memory.json"
      via: "loads memory JSON"
---

<objective>
Create test scaffolds for Phase 7 report generation. These tests define the expected output formats before implementation begins (TDD approach matching Phase 5-6 pattern).

Purpose: Establish contract for what report generation must produce before writing any implementation code.
Output: tests/test_report_generation.py with 6 test classes, tests/conftest.py with fixtures, tests/fixtures/report-memory.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-report-generation/07-RESEARCH.md
@.planning/templates/report-generator.py
@tests/conftest.py

# From RESEARCH.md:
# - Test framework: pytest
# - Phase Requirements to Test Map:
#   - RPT-01: Generates valid LaTeX from memory JSON
#   - RPT-02: Compiles LaTeX to PDF via xelatex
#   - RPT-03: Fixed outline structure with dynamic Task chapters
#   - RPT-04: mcmthesis/cumcmthesis template selection
#   - RPT-05: Chapter relevance map filters context correctly
#   - RPT-06: LLM output contains no Markdown, only LaTeX
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_report_generation.py with 6 test classes</name>
  <files>tests/test_report_generation.py</files>
  <read_first>
    - tests/conftest.py (existing fixtures pattern)
    - .planning/templates/report-generator.py (OutlineGenerator, LatexDocumentAssembler classes)
  </read_first>
  <action>
    Create tests/test_report_generation.py with the following test classes and methods:

1. **TestOutlineStructure** (RPT-03):
   - test_outline_with_single_task: Creates outline for 1 task, verifies structure
   - test_outline_with_multiple_tasks: Creates outline for 3 tasks, verifies dynamic Task N chapters
   - test_outline_fixed_sections_present: Verifies Problem Restatement, Model Assumptions, Problem Analysis, Solution, Conclusion, Appendix exist
   - test_task_chapters_dynamic: Verifies Task 1-N Analysis and Task 1-N Solution chapters are generated dynamically

2. **TestChapterRelevance** (RPT-05):
   - test_relevance_map_generated: Calls generate_chapter_relevance_map(3), verifies dict returned
   - test_model_setup_relevance: Verifies Model Setup only depends on corresponding Task Analysis
   - test_model_calculation_relevance: Verifies Model Calculation depends on Task Analysis + Model Setup
   - test_conclusion_relevance: Verifies Model Conclusion references all Task Solutions

3. **TestLatexGeneration** (RPT-01):
   - test_latex_preamble_mcm: Creates preamble for mcm template, verifies \documentclass{mcmthesis}
   - test_latex_preamble_cumcm: Creates preamble for cumcm template, verifies \documentclass{cumcmthesis}
   - test_latex_document_structure: Verifies document has \begin{document} and \end{document}
   - test_chapter_content_no_markdown: Generates chapter content, verifies no Markdown syntax (*, #, -)

4. **TestTemplateSelection** (RPT-04):
   - test_mcmthesis_class: Verifies mcmthesis documentclass used when template='mcm'
   - test_cumcmthesis_class: Verifies cumcmthesis documentclass used when template='cumcm'
   - test_mcm_setup_parameters: Verifies \mcmsetup with tcn, problem, year

5. **TestScientificWriting** (RPT-06):
   - test_no_markdown_headers: Content should not contain # headers
   - test_no_bullet_points: Content should not contain - or * bullet lists
   - test_continuous_narrative: Content should be paragraph-based
   - test_latex_commands_present: Content should contain \section, \subsection, or similar

6. **TestPDFCompilation** (RPT-02):
   - test_pdf_compilation_command: FileManager.generate_pdf() should call xelatex twice
   - test_pdf_output_exists: After compilation, .pdf file should exist
   - test_xelatex_available: Verify xelatex is available in environment

All test methods should use pytest assertions. Use unittest.mock for LLM interactions where needed. Each test class should have clear docstrings explaining what RPT it addresses.

Import from the skeleton: Chapter from report_generator (or define locally if skeleton not yet adapted).
  </action>
  <verify>
    <automated>pytest tests/test_report_generation.py -x -v --collect-only</automated>
  </verify>
  <done>tests/test_report_generation.py exists with 6 test classes (TestOutlineStructure, TestChapterRelevance, TestLatexGeneration, TestTemplateSelection, TestScientificWriting, TestPDFCompilation) totaling 20+ test methods</done>
</task>

<task type="auto">
  <name>Task 2: Add report generation fixtures to tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (existing pattern)
  </read_first>
  <action>
    Add the following fixtures to tests/conftest.py (append, do not overwrite existing):

```python
# ---- Phase 7: Report Generation Fixtures ----

@pytest.fixture
def sample_report_memory():
    """
    Returns a valid Memory structure for Phase 7 report generation testing.
    
    Schema matches the combined output of Phases 2-6:
    {
      "title": "...",
      "summary": "...",
      "keywords": "...",
      "problem_background": "...",
      "problem_requirement": "...",
      "problem_analysis": "...",
      "tasks": [
        {
          "task_id": "1",
          "task_description": "...",
          "task_analysis": "...",
          "preliminary_formulas": "...",
          "mathematical_modeling_process": "...",
          "execution_result": "...",
          "solution_interpretation": "...",
          "subtask_outcome_analysis": "..."
        }
      ]
    }
    """
    return {
        "title": "Urban Traffic Flow Optimization Using Queueing Theory",
        "summary": "This paper addresses the optimization of urban traffic light timing...",
        "keywords": "traffic optimization; queueing theory; genetic algorithm",
        "problem_background": "Urban traffic congestion has become a critical issue...",
        "problem_requirement": "Optimize traffic light timing at a 4-way intersection...",
        "problem_analysis": "The problem requires modeling vehicle arrival patterns...",
        "tasks": [
            {
                "task_id": "1",
                "task_description": "Model vehicle arrival patterns using Poisson distribution",
                "task_analysis": "Vehicle arrivals follow a Poisson process with rate lambda...",
                "preliminary_formulas": "P(N=k) = (lambda^k * e^-lambda) / k!",
                "mathematical_modeling_process": "We model the intersection as an M/M/1 queue...",
                "execution_result": "Average wait time: 45.2 seconds",
                "solution_interpretation": "The model predicts wait times with 92% accuracy...",
                "subtask_outcome_analysis": "Model performs well under low traffic conditions..."
            },
            {
                "task_id": "2",
                "task_description": "Optimize traffic light timing using genetic algorithm",
                "task_analysis": "GA can find near-optimal timing parameters...",
                "preliminary_formulas": "fitness = 1 / (1 + total_delay)",
                "mathematical_modeling_process": "We encode timing as chromosomes and evolve...",
                "execution_result": "Optimal cycle: 90s, green phases: [30, 25, 20, 15]",
                "solution_interpretation": "GA convergence achieved in 150 generations...",
                "subtask_outcome_analysis": "Solution reduces average wait time by 23%..."
            },
            {
                "task_id": "3",
                "task_description": "Validate model with real-world data",
                "task_analysis": "We validate using traffic counts from sensors...",
                "preliminary_formulas": "MAPE = (1/n) * sum(|actual-predicted|/actual)",
                "mathematical_modeling_process": "Validation uses 24-hour sensor data...",
                "execution_result": "MAPE = 8.3%, R^2 = 0.91",
                "solution_interpretation": "Model shows good fit with real traffic patterns...",
                "subtask_outcome_analysis": "Model generalizes well to different time periods..."
            }
        ]
    }


@pytest.fixture
def sample_metadata():
    """
    Returns sample metadata for report generation.
    
    Used to test LatexDocumentAssembler._create_preamble() and _create_abstract().
    """
    return {
        "title": "Urban Traffic Flow Optimization Using Queueing Theory",
        "team": "2500001",
        "year": "2025",
        "problem_type": "A",
        "summary": "This paper presents an optimization approach for urban traffic light timing...",
        "keywords": "traffic optimization; queueing theory; genetic algorithm"
    }


@pytest.fixture
def template_paths():
    """
    Returns paths to mcmthesis and cumcmthesis template directories.
    """
    import os
    templates_dir = os.path.join(os.path.dirname(os.path.dirname(__file__)), ".planning", "templates")
    return {
        "mcmthesis": os.path.join(templates_dir, "mcmthesis"),
        "cumcmthesis": os.path.join(templates_dir, "cumcmthesis")
    }
```
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'tests'); import conftest; print('conftest.py imports successfully')"</automated>
  </verify>
  <done>tests/conftest.py contains sample_report_memory, sample_metadata, and template_paths fixtures</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/fixtures/report-memory.json</name>
  <files>tests/fixtures/report-memory.json</files>
  <read_first>
    - tests/fixtures/sample-model.md (to understand memory structure)
  </read_first>
  <action>
    Create tests/fixtures/report-memory.json with a complete multi-task memory structure:

```json
{
  "title": "Urban Traffic Flow Optimization Using Queueing Theory",
  "summary": "This paper addresses the optimization of urban traffic light timing at a major intersection using a combination of queueing theory and genetic algorithms.",
  "keywords": "traffic optimization; queueing theory; genetic algorithm; M/M/1 queue",
  "problem_background": "Urban traffic congestion has become a critical issue in modern cities. This study focuses on optimizing traffic light timing at a 4-way intersection to minimize average vehicle wait times.",
  "problem_requirement": "Given traffic flow data and intersection geometry, determine optimal traffic light timing parameters to minimize total delay.",
  "problem_analysis": "The problem requires modeling vehicle arrivals as stochastic processes, optimizing timing parameters, and validating with real-world data.",
  "tasks": [
    {
      "task_id": "1",
      "task_description": "Model vehicle arrival patterns using Poisson distribution",
      "task_analysis": "Vehicle arrivals at the intersection follow a Poisson process. The arrival rate varies by time of day, with peak hours showing rates up to 60 vehicles per minute.",
      "preliminary_formulas": "Poisson distribution: P(N=k) = (lambda^k * e^(-lambda)) / k!",
      "mathematical_modeling_process": "We model the intersection as an M/M/1 queue where vehicles arrive according to Poisson process with rate lambda and service rate mu. The average wait time is W = 1/(mu - lambda).",
      "execution_result": "Average wait time: 45.2 seconds, utilization: 0.78",
      "solution_interpretation": "The queueing model accurately predicts wait times under varying traffic conditions. The model performs best when arrival rates are below 85% of service capacity.",
      "subtask_outcome_analysis": "Model validation shows 92% accuracy in predicting wait times during off-peak hours."
    },
    {
      "task_id": "2",
      "task_description": "Optimize traffic light timing using genetic algorithm",
      "task_analysis": "The optimization problem has multiple parameters: cycle length, green phase durations, and phase sequence. GA is suitable due to the complex solution space.",
      "preliminary_formulas": "Fitness function: f = 1 / (1 + total_delay), where total_delay = sum(wait_time for all vehicles)",
      "mathematical_modeling_process": "We encode timing parameters as chromosomes with real-valued genes. Selection uses tournament selection, crossover uses SBX operator. Mutation rate: 0.1, population size: 100.",
      "execution_result": "Optimal cycle: 90s, green phases: [30, 25, 20, 15] seconds, best fitness: 0.0042",
      "solution_interpretation": "GA converged after 150 generations. The solution reduces average wait time by 23% compared to fixed-timing baseline.",
      "subtask_outcome_analysis": "GA solution is robust across multiple random seeds, with fitness variance < 5%."
    },
    {
      "task_id": "3",
      "task_description": "Validate model with real-world traffic data",
      "task_analysis": "Validation requires comparing model predictions with actual traffic measurements from sensors at the intersection.",
      "preliminary_formulas": "MAPE = (1/n) * sum(|actual - predicted| / actual), R^2 = 1 - SS_res / SS_tot",
      "mathematical_modeling_process": "We collected 24-hour traffic counts from loop detectors. Data was aggregated into 5-minute intervals and compared with model predictions.",
      "execution_result": "MAPE = 8.3%, R^2 = 0.91, correlation = 0.95",
      "solution_interpretation": "The model shows good agreement with real traffic data, particularly during peak hours when traffic follows Poisson assumptions most closely.",
      "subtask_outcome_analysis": "Model performance is consistent across different days, indicating good generalization."
    }
  ]
}
```
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('tests/fixtures/report-memory.json')); print('Valid JSON')"</automated>
  </verify>
  <done>tests/fixtures/report-memory.json exists with valid JSON containing title, summary, keywords, problem_background, problem_requirement, problem_analysis, and 3 tasks</done>
</task>

</tasks>

<verification>
After all tasks:
1. Run `pytest tests/test_report_generation.py -x -v --collect-only` to verify all 6 test classes are discovered
2. Verify each test class has at least 3 test methods
3. Verify fixtures can be loaded without errors
</verification>

<success_criteria>
- tests/test_report_generation.py created with 6 test classes covering RPT-01~06
- tests/conftest.py updated with 3 new fixtures (sample_report_memory, sample_metadata, template_paths)
- tests/fixtures/report-memory.json created with valid multi-task memory structure
- All tests are discoverable by pytest
</success_criteria>

<output>
After completion, create `.planning/phases/07-report-generation/07-01-SUMMARY.md`
</output>

---
phase: 07-report-generation
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - .claude/skills/mm-agent/coordinator.md
  - .planning/memory/problem.md
autonomous: true
requirements:
  - RPT-01
  - RPT-02

must_haves:
  truths:
    - "Coordinator invokes report-generation skill after Phase 6"
    - "Memory data is loaded and passed to report generator"
    - "PDF is produced at .planning/output/report.pdf"
  artifacts:
    - path: ".claude/skills/mm-agent/coordinator.md"
      provides: "Phase 7 invocation in workflow"
  key_links:
    - from: "coordinator.md"
      to: "report-generation.md"
      via: "Skill invocation after Phase 6"
    - from: "coordinator.md"
      to: ".planning/memory/"
      via: "loads task-*.json files"
---

<objective>
Integrate report generation into the coordinator workflow. After Phase 6 (Code Execution) completes, the coordinator should invoke the report-generation skill to produce the final PDF.

Purpose: Connect Phase 7 to the end of the workflow pipeline.
Output: Updated coordinator.md with Phase 7 invocation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.claude/skills/mm-agent/coordinator.md
@.claude/skills/mm-agent/report-generation.md
@src/scripts/report_generator.py

# Phase 6 completes with:
# - .planning/memory/task-{id}.json for each task (contains modeling + execution results)
# - .planning/output/plots/*.png visualization files
# - .planning/output/code/*.py code files

# Phase 7 needs:
# - Load all task memories
# - Collect figure and code paths
# - Invoke PaperGenerator.generate_paper()
# - Output .planning/output/report.tex and .planning/output/report.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update coordinator.md to invoke Phase 7 report generation</name>
  <files>.claude/skills/mm-agent/coordinator.md</files>
  <read_first>
    - .claude/skills/mm-agent/coordinator.md (existing workflow)
    - .planning/memory/problem.md (sample structure)
  </read_first>
  <action>
    Read coordinator.md and find the location where Phase 6 (Code Execution) completes. Add a Phase 7 section after it.

The Phase 6 completion section should look like this (or similar):
```
## Phase 6: Code Generation & Execution
...
After Phase 6 completes:
- Results saved to .planning/memory/results-{task_id}.json
- Plots saved to .planning/output/plots/
- Code saved to .planning/output/code/
```

Add Phase 7 section:

```markdown
## Phase 7: Report Generation

After Phase 6 completes, invoke the report-generation skill to produce the final PDF.

### Step 7.1: Prepare Report Metadata

Collect metadata from problem.md and execution results:

```python
import json
import os
from pathlib import Path

# Load problem.md for title, summary, keywords
problem_path = Path(".planning/memory/problem.md")
if problem_path.exists():
    # Parse YAML frontmatter if present, otherwise use defaults
    metadata = {
        "title": "Mathematical Modeling Paper",
        "summary": "This paper presents solutions to...",
        "keywords": "optimization; modeling; simulation",
        "team": "2500001",
        "year": "2025",
        "problem_type": "A",
        "template": "mcm"  # or "cumcm" based on problem
    }

# Load task memories
memory_dir = Path(".planning/memory")
task_files = sorted(memory_dir.glob("task-*.json"))
tasks = []
for task_file in task_files:
    with open(task_file) as f:
        tasks.append(json.load(f))

# Collect figures and code
plots_dir = Path(".planning/output/plots")
codes_dir = Path(".planning/output/code")
metadata["figures"] = [str(f) for f in plots_dir.glob("*.png")] if plots_dir.exists() else []
metadata["codes"] = [str(f) for f in codes_dir.glob("*.py")] if codes_dir.exists() else []

# Combine into json_data
json_data = {
    "title": metadata.get("title", ""),
    "summary": metadata.get("summary", ""),
    "keywords": metadata.get("keywords", ""),
    "problem_background": tasks[0].get("problem_background", "") if tasks else "",
    "problem_requirement": tasks[0].get("problem_requirement", "") if tasks else "",
    "problem_analysis": tasks[0].get("problem_analysis", "") if tasks else "",
    "tasks": [
        {
            "task_id": t.get("task_id", ""),
            "task_description": t.get("task_description", ""),
            "task_analysis": t.get("task_analysis", ""),
            "preliminary_formulas": t.get("preliminary_formulas", ""),
            "mathematical_modeling_process": t.get("mathematical_modeling_process", ""),
            "execution_result": t.get("execution_result", ""),
            "solution_interpretation": t.get("solution_interpretation", ""),
            "subtask_outcome_analysis": t.get("subtask_outcome_analysis", "")
        }
        for t in tasks
    ]
}
```

### Step 7.2: Invoke Report Generation

Use the Skill tool to invoke report-generation:

```markdown
## Report Generation

Using the report-generation skill to generate the final paper...

[Skill: report-generation]
```

Or use the script directly if Skill tool not available:

```python
from src.scripts.report_generator import PaperGenerator

# Ensure output directory exists
os.makedirs(".planning/output", exist_ok=True)

# Generate report
generator = PaperGenerator(llm=None)  # LLM provided by environment
generator.generate_paper(
    json_data=json_data,
    metadata=metadata,
    output_dir=".planning/output",
    filename="report"
)

# Verify output
report_pdf = Path(".planning/output/report.pdf")
if report_pdf.exists():
    print(f"Report generated: {report_pdf}")
else:
    print("Warning: PDF not generated, check .planning/output/report.tex for LaTeX source")
```

### Step 7.3: Verify Report Generation

After Phase 7 completes:
- `.planning/output/report.tex` exists (LaTeX source)
- `.planning/output/report.pdf` exists (compiled PDF, may be 0 bytes if compilation failed)
- `.planning/output/appendix/` contains code and figures

Report any compilation errors to the user for manual intervention.
```

Find the exact location in coordinator.md (after Phase 6 section, before any closing remarks) and insert this Phase 7 content.
</action>
  <verify>
    <automated>grep -c "Phase 7\|Report Generation\|report-generation" .claude/skills/mm-agent/coordinator.md</automated>
  </verify>
  <done>coordinator.md contains Phase 7: Report Generation section with Step 7.1 (Prepare Metadata), Step 7.2 (Invoke Report Generation), Step 7.3 (Verify Output)</done>
</task>

<task type="auto">
  <name>Task 2: Create .planning/memory/problem.md fixture for testing</name>
  <files>.planning/memory/problem.md</files>
  <read_first>
    - .planning/memory/problem.md (if exists)
  </read_first>
  <action>
    Create a sample problem.md file in .planning/memory/ for testing report generation. This file represents the output of Phase 2 (Problem Analysis) and is used by Phase 7 to populate report metadata.

```markdown
---
title: Urban Traffic Flow Optimization Using Queueing Theory
team: 2500001
year: 2025
problem_type: A
template: mcm
---

# Problem Analysis

## Title

Urban Traffic Flow Optimization Using Queueing Theory

## Background

Urban traffic congestion has become a critical issue in modern cities. This study focuses on optimizing traffic light timing at a 4-way intersection to minimize average vehicle wait times while ensuring safety and fairness across different traffic flows.

## Questions

1. Model vehicle arrival patterns at the intersection using appropriate probability distributions
2. Optimize traffic light timing parameters to minimize total vehicle delay
3. Validate the proposed model with real-world traffic data

## Constraints

- Traffic light cycle must not exceed 120 seconds
- Minimum green phase duration: 15 seconds
- Must handle peak traffic volumes up to 60 vehicles per minute

## Objectives

- Minimize average vehicle wait time
- Maximize intersection throughput
- Ensure fair distribution of green time across directions

## Keywords

traffic optimization; queueing theory; genetic algorithm; M/M/1 queue; traffic light timing

## Summary

This paper addresses the optimization of urban traffic light timing using a combination of queueing theory and genetic algorithms. We model vehicle arrivals as a Poisson process and optimize timing parameters through evolutionary computation. The proposed approach reduces average wait times by 23% compared to fixed-timing baseline.
```

This file provides the metadata needed for report generation when problem.md exists. If the file already exists, update it to include title, team, year, problem_type, template, and summary fields.
</action>
  <verify>
    <automated>python -c "import yaml; print('YAML parse OK' if yaml.safe_load(open('.planning/memory/problem.md')) else 'FAIL')" 2>/dev/null || python -c "print('problem.md created')"</automated>
  </verify>
  <done>.planning/memory/problem.md exists with title, summary, keywords, background, questions, constraints, objectives</done>
</task>

</tasks>

<verification>
After all tasks:
1. Verify coordinator.md contains "Phase 7" section
2. Verify coordinator.md references "report-generation" skill
3. Verify problem.md fixture exists
</verification>

<success_criteria>
- coordinator.md updated with Phase 7: Report Generation section
- Step 7.1 shows how to load task memories and collect figures/codes
- Step 7.2 shows how to invoke report generation
- Step 7.3 shows how to verify output
- .planning/memory/problem.md created/updated with required metadata fields
</success_criteria>

<output>
After completion, create `.planning/phases/07-report-generation/07-03-SUMMARY.md`
</output>

---
phase: 01-claude-code-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.claude/skills/mm-agent/SKILL.md, .claude/skills/mm-agent/coordinator.md]
autonomous: true
requirements: [INTG-03]
user_setup: []
must_haves:
  truths:
    - "User can invoke /mm-agent command and receive initial workflow response"
    - "Skill is auto-discovered by Claude Code CLI"
    - "Skill accepts --problem parameter for problem file path"
  artifacts:
    - path: ".claude/skills/mm-agent/SKILL.md"
      provides: "Main workflow entry point"
      contains: "name: mm-agent"
      min_lines: 50
    - path: ".claude/skills/mm-agent/coordinator.md"
      provides: "Workflow orchestration logic"
      contains: "workflow orchestration"
      min_lines: 30
  key_links:
    - from: ".claude/skills/mm-agent/SKILL.md"
      to: ".claude/skills/mm-agent/coordinator.md"
      via: "Skill tool invocation"
      pattern: "Skill.*coordinator"
---

<objective>
Create the main mm-agent Skill as the workflow entry point, with coordinator sub-skill for orchestration.

Purpose: Users invoke `/mm-agent --problem <file>` to start the mathematical modeling workflow. This establishes the primary interface point inheriting Claude Code configuration.

Output: Working Skill with auto-discovery and parameter parsing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-claude-code-integration/01-CONTEXT.md
@.planning/phases/01-claude-code-integration/01-RESEARCH.md
@IDEA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main mm-agent SKILL.md entry point</name>
  <files>.claude/skills/mm-agent/SKILL.md</files>
  <read_first>
    - .claude/skills/mm-agent/SKILL.md (check if exists)
    - .planning/phases/01-claude-code-integration/01-CONTEXT.md (parameter decisions)
    - .planning/phases/01-claude-code-integration/01-RESEARCH.md (Skill structure reference)
  </read_first>
  <action>
    Create SKILL.md with the following structure:

    Frontmatter:
    ```yaml
    ---
    name: mm-agent
    description: Mathematical modeling workflow - parse problems, build models, execute simulations, generate reports
    ---
    ```

    Objective section:
    ```markdown
    <objective>
    Execute end-to-end mathematical modeling workflow from problem input to paper report output.
    Users invoke via /mm-agent --problem <file> to start the 7-phase pipeline.

    Phases:
    1. Problem Analysis - Parse and structure competition problems
    2. Task Decomposition - Build DAG with dependencies
    3. HMML Knowledge Retrieval - Find relevant modeling methods
    4. Mathematical Modeling - Generate models via Actor-Critic iteration
    5. Code Generation & Execution - Run Python numerical simulations
    6. Report Generation - Produce LaTeX/PDF paper reports

    The workflow inherits Claude Code's model configuration automatically.
    </objective>
    ```

    Execution context: No references needed for Wave 1 (referenced in Wave 2)

    Process section:
    ```markdown
    <process>
    ## Step 1: Parse command arguments
    Use Bash tool to extract $ARGUMENTS and parse parameters:
    - --problem <file> (required) - Path to problem file (PDF/MD/TXT)
    - --interactive (optional) - Step-by-step confirmation mode
    - --skip-verify (optional) - Disable verification gates
    - --phase N (optional) - Resume from phase N

    Store parsed values in variables for downstream use.

    ## Step 2: Validate problem file exists
    Use Bash tool to check:
    - File exists at specified path
    - File extension is supported (.pdf, .md, .txt)
    - File is readable

    If validation fails, report error with supported format list and exit.

    ## Step 3: Invoke coordinator skill
    Use Skill tool to load coordinator.md for workflow orchestration.
    Pass parsed arguments as context.

    Coordinator skill handles:
    - GSD phase orchestration
    - Memory system initialization
    - Agent invocation per phase
    - Progress tracking and reporting
    </process>
    ```

    The skill should be concise (~60-80 lines) with clear step-by-step instructions.
  </action>
  <verify>
    <automated>test -f .claude/skills/mm-agent/SKILL.md && grep -q "^name: mm-agent$" .claude/skills/mm-agent/SKILL.md && grep -q "<objective>" .claude/skills/mm-agent/SKILL.md && grep -q "<process>" .claude/skills/mm-agent/SKILL.md && grep -q "Task 1: Parse" .claude/skills/mm-agent/SKILL.md && wc -l < .claude/skills/mm-agent/SKILL.md | awk '{print $1}' | xargs -I {} test {} -ge 50</automated>
  </verify>
  <done>SKILL.md exists with valid frontmatter (name, description), objective section explaining 7-phase pipeline, process section with 3 steps (parse arguments, validate file, invoke coordinator), and minimum 50 lines of content</done>
</task>

<task type="auto">
  <name>Task 2: Create coordinator sub-skill for workflow orchestration</name>
  <files>.claude/skills/mm-agent/coordinator.md</files>
  <read_first>
    - .claude/skills/mm-agent/coordinator.md (check if exists)
    - .claude/skills/mm-agent/SKILL.md (passing arguments pattern)
    - .planning/ROADMAP.md (phase definitions)
    - IDEA.md (DAG and Memory system design)
  </read_first>
  <action>
    Create coordinator.md with the following structure:

    Frontmatter:
    ```yaml
    ---
    name: coordinator
    description: Workflow orchestrator for mm-agent phases
    ---
    ```

    Objective section:
    ```markdown
    <objective>
    Orchestrate the 7-phase mathematical modeling workflow using GSD framework.

    Responsibilities:
    - Initialize Memory system (.planning/memory/ directory)
    - Execute phases sequentially with context isolation
    - Manage DAG-based task dependencies
    - Track progress and report status
    - Handle errors and recovery

    This skill is invoked by the main mm-agent SKILL.md after parameter parsing.
    </objective>
    ```

    Execution context:
    ```markdown
    <execution_context>
    @.planning/ROADMAP.md
    @IDEA.md
    @.claude/skills/mm-agent/SKILL.md
    </execution_context>
    ```

    Process section:
    ```markdown
    <process>
    ## Step 1: Initialize Memory system
    Use Bash tool to create .planning/memory/ directory if not exists.
    This directory stores:
    - dag.json - Task dependency graph
    - execution-order.txt - Topological sort result
    - task-*.json - Individual task memories
    - retrieved-methods.json - HMML search results

    Create directory structure:
    ```bash
    mkdir -p .planning/memory
    mkdir -p .planning/code
    mkdir -p .planning/output
    mkdir -p tests/fixtures
    ```

    ## Step 2: Report workflow start
    Display progress banner:
    ```
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     MM-Agent ► Mathematical Modeling Workflow
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    Problem file: {parsed_problem_path}
    Mode: {interactive/auto}
    Phases: 7 total
    ```

    ## Step 3: Invoke GSD phases (Phase 1: Foundation)
    For Phase 1 (Claude Code Integration), this is the setup phase.

    For future phases (2-7), use GSD workflow:
    ```bash
    /gsd:execute-phase 02-problem-analysis
    /gsd:execute-phase 03-task-decomposition
    /gsd:execute-phase 04-hmml-retrieval
    /gsd:execute-phase 05-mathematical-modeling
    /gsd:execute-phase 06-code-execution
    /gsd:execute-phase 07-report-generation
    ```

    Each phase produces artifacts in .planning/memory/ and passes context to next phase.

    ## Step 4: Report workflow completion
    After all phases complete:
    ```
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     Workflow Complete
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    Output: .planning/output/report.pdf
    Memory: .planning/memory/task-*.json
    ```

    Note: Phase 1 is the foundation phase. Actual workflow execution happens in Phase 2+.
    </process>
    ```

    The coordinator skill should focus on orchestration logic (~40-60 lines).
  </action>
  <verify>
    <automated>test -f .claude/skills/mm-agent/coordinator.md && grep -q "^name: coordinator$" .claude/skills/mm-agent/coordinator.md && grep -q "<objective>" .claude/skills/mm-agent/coordinator.md && grep -q "<execution_context>" .claude/skills/mm-agent/coordinator.md && grep -q "Memory system" .claude/skills/mm-agent/coordinator.md && grep -q "mkdir -p .planning/memory" .claude/skills/mm-agent/coordinator.md && wc -l < .claude/skills/mm-agent/coordinator.md | awk '{print $1}' | xargs -I {} test {} -ge 30</automated>
  </verify>
  <done>coordinator.md exists with valid frontmatter (name, description), objective section explaining orchestration responsibilities, execution context referencing ROADMAP.md and IDEA.md, process section with 4 steps (initialize Memory, report start, invoke GSD phases, report completion), directory creation commands for memory/code/output/fixtures, and minimum 30 lines of content</done>
</task>

</tasks>

<verification>
1. Skill files exist: `.claude/skills/mm-agent/SKILL.md` and `.claude/skills/mm-agent/coordinator.md`
2. Both files have valid frontmatter with name and description fields
3. SKILL.md references coordinator.md in process section
4. coordinator.md creates required directory structure
5. No external dependencies or configuration files needed
</verification>

<success_criteria>
1. User can run `/mm-agent --problem tests/fixtures/simple.md` and receive initial workflow response
2. Skill is auto-discovered by Claude Code (no manual registration needed)
3. Parameters (--problem, --interactive, --skip-verify, --phase) are parsed and validated
4. Required directories (.planning/memory, .planning/code, .planning/output, tests/fixtures) are created
5. Workflow banner and progress messages are displayed
</success_criteria>

<output>
After completion, create `.planning/phases/01-claude-code-integration/01-01-SUMMARY.md` with:
- Files created: SKILL.md, coordinator.md
- Key patterns established: Skill naming, frontmatter format, process structure
- Parameter parsing implementation details
- Directory structure created
</output>
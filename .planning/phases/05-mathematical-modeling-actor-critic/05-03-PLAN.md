---
phase: 05-mathematical-modeling-actor-critic
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: [.claude/skills/mm-agent/coordinator.md]
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05]
user_setup: []

must_haves:
  truths:
    - "Coordinator invokes modeling skill after HMML retrieval (Step 4.5.5)"
    - "Coordinator passes task description and retrieved methods to modeling skill"
    - "Coordinator reads model-{task_id}.md and formulas-{task_id}.json after modeling completes"
    - "Coordinator updates task memory with modeling results"
  artifacts:
    - path: ".claude/skills/mm-agent/coordinator.md"
      provides: "Coordinator with Phase 5 (Mathematical Modeling) integration"
      min_lines: 500
      contains: "Step 4.5.5.*Mathematical Modeling"
  key_links:
    - from: ".claude/skills/mm-agent/coordinator.md"
      to: ".claude/skills/mm-agent/modeling.md"
      via: "Invoke modeling skill in task execution loop"
      pattern: "modeling.*md|Step 4\\.5\\.5"
    - from: ".claude/skills/mm-agent/coordinator.md"
      to: ".planning/memory/model-{task_id}.md"
      via: "Read modeling output after skill invocation"
      pattern: "model-.*\\.md"
    - from: ".claude/skills/mm-agent/coordinator.md"
      to: ".planning/memory/formulas-{task_id}.json"
      via: "Read formulas.json after skill invocation"
      pattern: "formulas-.*\\.json"
---

<objective>
Integrate mathematical modeling skill into coordinator workflow.

Purpose: Add Step 4.5.5 to coordinator task execution loop, invoking the modeling skill after HMML retrieval and passing outputs to Phase 6.

Output: Updated coordinator.md with Phase 5 integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@IDEA.md §8 (Actor-Critic iteration, integration context)
@.claude/skills/mm-agent/coordinator.md (existing workflow with Step 4.5.4 HMML retrieval)
@.claude/skills/mm-agent/modeling.md (modeling skill from Plan 02)
@.planning/ROADMAP.md (Phase 5 success criteria)
@.claude/skills/mm-agent/parse-problem.md (skill invocation pattern)
@.planning/phases/04-hmml-knowledge-retrieval/04-04-PLAN.md (Phase 4 integration pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate modeling skill into coordinator workflow</name>
  <files>.claude/skills/mm-agent/coordinator.md</files>
  <read_first>
    .claude/skills/mm-agent/coordinator.md - Existing workflow, especially Step 4.5.4 (HMML retrieval) and task execution loop
    .claude/skills/mm-agent/modeling.md - Modeling skill interface (inputs, outputs, invocation)
    IDEA.md §8 - For Actor-Critic iteration parameters (max_rounds=3, satisfaction_threshold=8)
    .claude/skills/mm-agent/parse-problem.md - For skill invocation pattern (Skill command, error handling)
    .planning/phases/04-hmml-knowledge-retrieval/04-04-PLAN.md - For Phase 4 integration pattern (script invocation, output reading)
  </read_first>
  <action>
    Update .claude/skills/mm-agent/coordinator.md by adding Step 4.5.5 after Step 4.5.4 (HMML retrieval):

    Insert this new step after the HMML retrieval section (around line 252):

    ```markdown
    ### Step 4.5.5: Mathematical Modeling with Actor-Critic

    # Generate mathematical modeling solution using Actor-Critic iteration
    echo "  Generating mathematical model..."

    # Invoke modeling skill
    # The skill will:
    # - Load task description and retrieved methods
    # - Perform Actor-Critic iteration (max_rounds=3, satisfaction_threshold=8)
    # - Output model.md and formulas.json
    MODELING=$(Skill modeling --task-id $TASK_ID || echo "FAILED")

    # Check modeling succeeded
    if [ "$MODELING" = "FAILED" ]; then
      echo "  ⚠ Warning: Mathematical modeling failed for task $TASK_ID"
      echo "  Continuing with basic solution..."
      # Create placeholder files for Phase 6
      echo "Modeling failed - basic placeholder" > .planning/memory/model-$TASK_ID.md
      echo '{"task_id":"'"$TASK_ID"'","equations":[],"variables":[],"assumptions":[]}' > .planning/memory/formulas-$TASK_ID.json
    else
      echo "  ✓ Mathematical modeling complete"

      # Verify model.md was created
      if [ -f ".planning/memory/model-$TASK_ID.md" ]; then
        MODEL_METHOD=$(grep "^# Modeling Method" .planning/memory/model-$TASK_ID.md -A 5 | tail -4 | head -1 | sed 's/^[[:space:]]*//')
        echo "     Method: ${MODEL_METHOD:0:60}..."
      else
        echo "  ⚠ Warning: model-$TASK_ID.md not created"
      fi

      # Verify formulas.json was created
      if [ -f ".planning/memory/formulas-$TASK_ID.json" ]; then
        FORMULA_COUNT=$(python3 -c "import json; print(len(json.load(open('.planning/memory/formulas-$TASK_ID.json'))['equations']))" 2>/dev/null || echo "0")
        echo "     Formulas: $FORMULA_COUNT equations"
      else
        echo "  ⚠ Warning: formulas-$TASK_ID.json not created"
      fi

      # Display iteration info (from frontmatter if available)
      FINAL_SCORE=$(python3 -c "
import yaml, sys
try:
    with open('.planning/memory/model-$TASK_ID.md') as f:
        # Parse frontmatter (between --- markers)
        lines = f.readlines()
        if lines[0].strip() == '---':
            i = 1
            fm_lines = []
            while i < len(lines) and lines[i].strip() != '---':
                fm_lines.append(lines[i])
                i += 1
            fm = yaml.safe_load(''.join(fm_lines))
            print(fm.get('final_score', 'N/A'))
            print(fm.get('iteration_rounds', 'N/A'))
except:
    print('N/A')
    print('N/A')
    " 2>/dev/null || echo "N/A")
      echo "     Score: $(echo "$FINAL_SCORE" | head -1)"
      echo "     Rounds: $(echo "$FINAL_SCORE" | tail -1)"
    fi

    # Update task memory with modeling results
    python3 << EOF
import json
import sys
from datetime import datetime

task_id = sys.argv[1]
memory_path = f".planning/memory/task-{task_id}.json"

try:
    with open(memory_path) as f:
        memory = json.load(f)

    # Update status and phase
    memory['status'] = 'completed'
    memory['phase'] = 'mathematical-modeling'
    memory['updated_at'] = datetime.now().isoformat()

    # Read model.md if exists
    try:
        with open(f".planning/memory/model-{task_id}.md", 'r', encoding='utf-8') as f:
            model_content = f.read()
            # Extract just the content (skip frontmatter)
            lines = model_content.split('\n')
            if lines[0].strip() == '---':
                idx = lines.index('---', 1) + 1
                memory['mathematical_modeling_process'] = '\n'.join(lines[idx:])
            else:
                memory['mathematical_modeling_process'] = model_content
    except FileNotFoundError:
        memory['mathematical_modeling_process'] = "Modeling failed"

    # Read formulas.json if exists
    try:
        with open(f".planning/memory/formulas-{task_id}.json", 'r', encoding='utf-8') as f:
            memory['preliminary_formulas'] = json.load(f)
    except FileNotFoundError:
        memory['preliminary_formulas'] = {"equations": [], "variables": [], "assumptions": []}

    with open(memory_path, 'w', encoding='utf-8') as f:
        json.dump(memory, f, indent=2, ensure_ascii=False)

    print(f"Updated task-{task_id}.json with modeling results")
except Exception as e:
    print(f"Error updating memory: {e}")
EOF
      "$TASK_ID"

    ```

    Also update the coordinator's context section to include Phase 5 outputs. Find the section around line 430 that lists phase outputs, and add:

    ```markdown
    **Phase 5 integration:**
    - Mathematical modeling (modeling.md skill)
    - Actor-Critic iteration with max_rounds=3, satisfaction_threshold=8
    - model.md (Modeling Method, Formulas, Variables, Assumptions)
    - formulas.json (equations[], variables[], assumptions[])

    **Phase 5 output:**
    - .planning/memory/model-{id}.md - Modeling method document
    - .planning/memory/formulas-{id}.json - Structured formula definitions
    ```

    Update the Phase 4.7 section to reflect that phases now include Phase 5:

    ```markdown
    ## Step 4.7: Execute Phase 5-7 (mm-agent internal mechanism)
    Execute subsequent phases using mm-agent internal phase sub-skills:

    ```bash
    # Phase 4: HMML Knowledge Retrieval
    # Uses hmml-retrieval.md sub-skill (already integrated in Step 4.5.4)

    # Phase 5: Mathematical Modeling
    # Uses modeling.md skill (integrated in Step 4.5.5)
    # Input: task-desc, retrieved-methods.json
    # Output: model.md, formulas.json

    # Phase 6: Code Generation & Execution
    # Uses code-execution.md agent
    # Input: model.md, formulas.json
    # Output: results.json, plots/

    # Phase 7: Report Generation
    # Uses report-generation.md agent
    # Input: all memory artifacts
    # Output: report.pdf (LaTeX compiled)
    ```

    Each phase produces artifacts in `.planning/memory/` and passes context to next phase.

    **Phase sub-skills location:**
    - `.claude/skills/mm-agent/parse-problem.md` — Phase 2 problem parsing
    - `.claude/skills/mm-agent/task-decomposition.md` — Phase 3 task decomposition
    - `.claude/skills/mm-agent/modeling.md` — Phase 5 Actor-Critic modeling
    - `.claude/skills/mm-agent/code-execution.md` — Phase 6 code generation
    - `.claude/skills/mm-agent/report-generation.md` — Phase 7 report compilation

    **Runtime independence:** mm-agent workflow executes phases internally via skill invocations and script calls, not via `/gsd:*` commands. GSD framework is used for development workflow (planning, verification), not runtime execution.

    Note: Phase 1 (Foundation) establishes the skills and agents. Phase 2 (Problem Analysis) parses the input file. Phase 3 (Task Decomposition) sets up DAG and context. Phase 4 (HMML Retrieval) retrieves relevant methods. Phase 5 (Mathematical Modeling) generates modeling solutions. Phases 6-7 execute in sequence using internal mechanism.
    ```

    Key specifications:
    - Step 4.5.5 added after Step 4.5.4 in task execution loop
    - Invokes modeling skill with --task-id parameter
    - Reads model.md and formulas.json after completion
    - Displays modeling method, formula count, iteration score
    - Updates task memory with mathematical_modeling_process and preliminary_formulas
    - Graceful error handling: continues with placeholders if modeling fails
  </action>
  <verify>
    grep -q "Step 4.5.5.*Mathematical Modeling" .claude/skills/mm-agent/coordinator.md
    grep -q "Skill modeling" .claude/skills/mm-agent/coordinator.md
    grep -q "model-\$TASK_ID.md" .claude/skills/mm-agent/coordinator.md
    grep -q "formulas-\$TASK_ID.json" .claude/skills/mm-agent/coordinator.md
  </verify>
  <acceptance_criteria>
    - Step 4.5.5 added to coordinator.md after Step 4.5.4
    - Step 4.5.5 invokes modeling skill with --task-id parameter
    - Step 4.5.5 reads model.md and formulas.json after modeling completes
    - Step 4.5.5 displays iteration score and rounds (from frontmatter)
    - Step 4.5.5 updates task memory with mathematical_modeling_process
    - Step 4.5.5 handles errors gracefully with placeholder outputs
    - Coordinator context section updated with Phase 5 outputs
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Verify Step 4.5.5 exists in coordinator
grep -A 50 "Step 4.5.5.*Mathematical Modeling" .claude/skills/mm-agent/coordinator.md | head -20

# Verify skill invocation
grep -q "Skill modeling" .claude/skills/mm-agent/coordinator.md

# Verify output reading
grep -q "model-\$TASK_ID.md" .claude/skills/mm-agent/coordinator.md
grep -q "formulas-\$TASK_ID.json" .claude/skills/mm-agent/coordinator.md

# Run tests for MODEL requirements
pytest tests/test_mathematical_modeling.py -x -v
```
</verification>

<success_criteria>
- Step 4.5.5 integrated into coordinator task execution loop
- Coordinator invokes modeling skill after HMML retrieval
- Coordinator reads and validates model.md and formulas.json
- Coordinator updates task memory with modeling results
- Error handling with graceful fallback to placeholders
- All MODEL-01 through MODEL-05 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-mathematical-modeling-actor-critic/05-03-SUMMARY.md`
</output>
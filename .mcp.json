{
  "mcpServers": {
    "hmml-server": {
      "command": "python",
      "args": ["servers/hmml-server/server.py"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}